const initialState = {
    user: {},
    isLoading: false,
    loggingIn: false,
    editMode: false,
    error: null,
    trips: []
};

const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default reducer;